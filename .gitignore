.env
.env.dev
__pycache__/
*.pyc

# PyInstaller build artifacts (dev launcher .exe)
build/
dist/
*.spec
TookBot Dev Launcher.exe
.claude/
.deepsec/
.impeccable/
github-profile/
assets/lol_emblems/
handoff.md

# YouTube cookies — sensitive (contient session login)
cookies.txt

# Etat runtime du bot (généré par le bot, lu par web.py)
bot_state.json

# Backups locaux
backups/

# SQLite databases — never version, contains live data
bot_database.db
*.db
*.db-journal
*.sqlite
*.sqlite3

# Lavalink : binaire, plugins, logs, config live (contient le refreshToken OAuth).
# On versionne uniquement lavalink/application.yml.example
lavalink/Lavalink.jar
lavalink/plugins/
lavalink/logs/
lavalink/application.yml